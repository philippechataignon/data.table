
PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS) -Wall
PKG_LIBS = $(SHLIB_OPENMP_CFLAGS) -llz4

all: $(SHLIB)
	mv $(SHLIB) datatable$(SHLIB_EXT)
	if [ "$(OS)" != "Windows_NT" ] && [ `uname -s` = 'Darwin' ]; then install_name_tool -id datatable$(SHLIB_EXT) datatable$(SHLIB_EXT); fi


